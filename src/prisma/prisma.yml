endpoint: http://localhost:7766/
datamodel:
  - models/post.graphql
  - models/user.graphql
generate:
  - generator: typescript-client
    output: ./generated/prisma-client/
# Seeding is Broken
# seed:
#   run: node seed.js

